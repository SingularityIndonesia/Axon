# TODO

## DI — Interface binding (`@Bind`)

Currently only concrete classes can be injected. Interface binding would allow a resolver or
service to declare a dependency on an interface, with the implementation resolved at compile
time via an explicit binding declaration.

No design or timeline yet — deferred until a concrete use case requires it.
